apiVersion: v1
kind: Service
metadata:
  name: prometheus-adapter
  namespace: prometheus
spec:
  selector:
    app: prometheus-adapter
  ports:
    - port: 443
      targetPort: 6443
      name: https
